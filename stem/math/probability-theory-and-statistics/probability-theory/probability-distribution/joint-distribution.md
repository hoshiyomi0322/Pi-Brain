# Joint Distribution

# Discrete Joint Distribution
- ### PMF：$`f(x,~y)=P(Y=y\mid X=x)P(X=x)=P(X=x\mid Y=y)P(Y=y)`$
    - $`f(x,~y)\ge 0`$
    - $`\sum\limits_x{f(x,~y)}=1`$
- ### CDF：

# Discrete Marginal Distribution